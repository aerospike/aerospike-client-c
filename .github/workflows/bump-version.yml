name: Bump Version
run-name: >-
  ${{ format('Bump version to {0}{1}', inputs.version, inputs.dry-run && ' (dry-run)' || '') }}

# This workflow commits the version bump only. Tagging happens after stage is
# merged to master via announce-release.yml.
#
# Full release flow:
#   1. Run bump-version.yml -> version commit on stage
#   2. Manually run push-to-stage.yml (C Client Build & Release) on that branch
#   3. Open PR from stage to master -> review -> merge
#   4. Run announce-release.yml to tag the merge-base commit and start release work

permissions:
  contents: write       # push version-bump branch
  pull-requests: write  # open PR into target branch

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Jfrog Release Bundle version (e.g. 7.5.0 or 7.5.0-rc1)'
        required: true
        type: string
      dry-run:
        description: 'Preview the change'
        required: false
        type: boolean
        default: true

defaults:
  run:
    shell: bash

jobs:
  bump:
    name: bump version
    runs-on: ubuntu-24.04
    env:
      TARGET_VERSION: ${{ inputs.version }}
      TARGET_BRANCH:  ${{ github.ref_name }}
      IS_DRY_RUN: ${{ inputs.dry-run && 'true' || 'false' }}

    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5 # v2.19.3
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          ref: ${{ env.TARGET_BRANCH }}
          fetch-depth: 1

      - name: Run pkg/set_version
        run: |
          set -euo pipefail
          echo "Setting version to: $TARGET_VERSION"
          if [ "$IS_DRY_RUN" = "true" ]; then
            echo "[dry-run] Would run: ./pkg/set_version $TARGET_VERSION"
            echo "Files that would be updated:"
            echo "  src/include/aerospike/version.h"
            echo "  src/main/aerospike/version.c"
            echo "  vs/aerospike-client-c.nuspec"
            echo "  vs/aerospike-client-c-libuv.nuspec"
            echo "  vs/aerospike-client-c-libevent.nuspec"
          else
            ./pkg/set_version "$TARGET_VERSION"
          fi

      - name: Commit version bump and open PR
        if: ${{ env.IS_DRY_RUN != 'true' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit — version already at $TARGET_VERSION"
            exit 0
          fi

          # Push to a separate branch so the protected target branch is not touched directly.
          BUMP_BRANCH="bump-version/${TARGET_VERSION}"
          git checkout -b "$BUMP_BRANCH"
          git commit -m "update version to $TARGET_VERSION"
          git push origin "$BUMP_BRANCH"
          echo "Pushed to $BUMP_BRANCH"

          # Open a draft PR into the target branch for review before merging.
          PR_URL=$(gh pr create \
            --base  "$TARGET_BRANCH" \
            --head  "$BUMP_BRANCH" \
            --title "update version to $TARGET_VERSION" \
            --body  "Automated version bump to \`$TARGET_VERSION\`. Review and merge before running \`push-to-stage.yml\`." \
            --draft)
          echo "PR opened: $PR_URL"

      - name: Summary
        run: |
          {
            echo "## Version Bump"
            echo ""
            echo "| Parameter | Value |"
            echo "| :--- | :--- |"
            echo "| Version | \`$TARGET_VERSION\` |"
            echo "| Branch  | \`$TARGET_BRANCH\` |"
            echo "| Dry run | \`$IS_DRY_RUN\` |"
            echo ""
            if [ "$IS_DRY_RUN" = "true" ]; then
              echo "> **Dry run** — no commits were made."
            else
              echo "- Opened draft PR: bump-version/\`$TARGET_VERSION\` → \`$TARGET_BRANCH\`."
              echo "- **Next:** review and merge the PR, then manually run \`push-to-stage.yml\` (C Client Build & Release) on \`$TARGET_BRANCH\` with \`dry-run: false\`."
              echo ""
              echo "_After the release build, open a PR from \`$TARGET_BRANCH\` to \`master\`._"
              echo "_When merged, run \`announce-release.yml\` to tag the commit and draft the GitHub release._"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
