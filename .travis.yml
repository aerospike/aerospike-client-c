language: c

compiler:
  - gcc
  - clang

os:
  - linux
  - osx

before_install:
  - sudo apt-get install libc6-dev
  - sudo apt-get install libssl-dev
  - sudo apt-get install liblua5.1-dev

script: make